Open a terminal or command prompt run python -m venv env # Replace "env" with your desired environment name
cd your_project_directory (go to project dir)
pip install -r requirements.txt (install requirements)
